{
  "name": "unisphere-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "server:dev": "npm run dev --prefix server",
    "server:start": "npm start --prefix server",
    "client:dev": "npm run dev --prefix client",
    "client:build": "npm run build --prefix client"
  },
  "dependencies": {
    "node-cron": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/node-cron": "^3.0.11"
  }
}
